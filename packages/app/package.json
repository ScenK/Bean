{
  "name": "@bean/app",
  "version": "0.8.12",
  "description": "A desktop companion that chats, remembers, and — with your confirmation — gets things done.",
  "author": "Scen.K",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node scripts/dev.mjs",
    "start": "electron dist/main.js",
    "dist:mac": "pnpm build && electron-builder --mac",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "postversion": "git push --follow-tags"
  },
  "build": {
    "appId": "dev.scenk.bean",
    "productName": "Bean",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**",
      "assets/**",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "../../.bean",
        "to": "builtin"
      },
      {
        "from": "../discord/dist",
        "to": "chatops/discord",
        "filter": [
          "server.js",
          "server.js.map"
        ]
      },
      {
        "from": "../teams/dist",
        "to": "chatops/teams",
        "filter": [
          "server.js",
          "server.js.map"
        ]
      }
    ],
    "mac": {
      "target": [
        "dmg",
        "zip"
      ],
      "category": "public.app-category.productivity",
      "extendInfo": {
        "LSUIElement": true
      }
    }
  },
  "devDependencies": {
    "@bean/core": "workspace:*",
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.1",
    "dompurify": "^3.4.11",
    "electron": "^43.1.0",
    "electron-builder": "^26.15.3",
    "esbuild": "^0.28.1",
    "marked": "^18.0.6",
    "preact": "^10.29.7",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
